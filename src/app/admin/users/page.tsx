"use client";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { api } from "@/lib/api";
type AdminUser={_id:string;name:string;email:string;role:string;status:"active"|"suspended";createdAt:string};
export default function AdminUsers(){const client=useQueryClient();const query=useQuery({queryKey:["admin-users"],queryFn:()=>api<AdminUser[]>("/api/v1/admin/users")});const mutation=useMutation({mutationFn:({id,status}:{id:string;status:"active"|"suspended"})=>api(`/api/v1/admin/users/${id}/status`,{method:"PATCH",body:JSON.stringify({status})}),onSuccess:()=>client.invalidateQueries({queryKey:["admin-users"]})});return <div className="shell py-10"><h1 className="text-3xl font-extrabold">Manage users</h1>{query.isLoading&&<p className="mt-7 text-slate-500">Loading users…</p>}{query.isError&&<p className="mt-7 text-rose-700">{query.error.message}</p>}{query.data&&<div className="card mt-7 overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs text-slate-500"><tr><th className="p-4">User</th><th>Email</th><th>Role</th><th>Status</th><th className="p-4">Action</th></tr></thead><tbody>{query.data.map(user=><tr className="border-t border-slate-100" key={user._id}><td className="p-4 font-bold">{user.name}</td><td>{user.email}</td><td className="capitalize">{user.role}</td><td className="capitalize">{user.status}</td><td className="p-4"><button disabled={mutation.isPending} onClick={()=>mutation.mutate({id:user._id,status:user.status==='active'?'suspended':'active'})} className="btn btn-secondary !min-h-8 text-xs">{user.status==='active'?'Suspend':'Restore'}</button></td></tr>)}</tbody></table></div>}</div>}
